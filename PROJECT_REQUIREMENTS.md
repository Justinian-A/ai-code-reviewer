# AI 代码评审工具 - 项目需求文档

## 一、项目概述

### 1.1 项目名称
AI Code Reviewer（AI 代码评审助手）

### 1.2 核心目标
构建一个以 AI 辅助分析为核心的 GitHub PR 代码评审工具，帮助开发者提升 Review 效率与质量。

### 1.3 核心价值
- 自动化 PR 变更分析，减少人工阅读时间
- 智能识别潜在风险代码，降低漏检率
- 提供结构化 Review 建议，提升评审质量

---

## 二、技术选型

| 层级 | 技术选择 | 说明 |
|------|----------|------|
| **前端** | React + TailwindCSS | 组件化开发，响应式设计 |
| **后端** | Python (FastAPI) | 异步高性能，AI 集成方便 |
| **数据库** | SQLite | 轻量级，无需额外部署 |
| **AI 模型** | MiMo（小米推理模型） | 代码理解与推理能力强 |
| **GitHub 交互** | Personal Access Token | 用户级认证，简单直接 |

---

## 三、功能需求

### 3.1 核心功能（P0 - 必须实现）

#### 3.1.1 PR 分析核心功能
- **输入**: GitHub PR 链接或仓库地址 + PR 编号
- **处理流程**:
  1. 通过 GitHub API 获取 PR 元数据（标题、描述、作者等）
  2. 获取代码变更（diff）和完整文件内容
  3. 获取 PR 评论和 Review 历史（上下文）
  4. 调用 AI 模型进行智能分析
- **输出**: 结构化分析报告

#### 3.1.2 可视化报告页面
- PR 基本信息展示（标题、作者、分支、状态）
- 变更文件列表与统计（新增/删除/修改行数）
- 代码变更高亮显示（diff 视图）
- AI 分析结果展示：
  - 📋 **变更总结**: PR 做了什么，为什么这样做
  - ⚠️ **风险标记**: 潜在问题（bug、安全、性能）
  - 💡 **改进建议**: 具体代码行的优化建议
- 风险等级标识（高/中/低）
- 支持按风险类型筛选

#### 3.1.3 历史记录管理
- 保存每次分析的完整记录
- 支持查看历史分析列表
- 支持对比不同 PR 的分析结果
- 支持删除历史记录

#### 3.1.4 用户认证系统
- 基础用户注册/登录
- GitHub Token 安全存储（加密）
- 用户个人设置管理

### 3.2 扩展功能（P1 - 后续迭代）

- [ ] CLI 命令行工具
- [ ] GitHub Webhook 自动触发分析
- [ ] 多模型切换支持
- [ ] 团队协作功能
- [ ] 分析结果导出（PDF/Markdown）
- [ ] 自定义分析规则

---

## 四、AI 分析能力设计

### 4.1 分析维度

| 维度 | 分析内容 | 输出示例 |
|------|----------|----------|
| **变更总结** | PR 做了什么、为什么这样做 | "本次 PR 重构了用户认证模块，引入 JWT 替代 Session..." |
| **代码质量** | 命名规范、代码结构、重复代码 | "函数名 `doStuff` 不够语义化，建议改为 `processUserData`" |
| **潜在 Bug** | 空指针、边界条件、逻辑错误 | "第 42 行未检查 null，可能导致 NPE" |
| **安全风险** | SQL 注入、XSS、敏感信息泄露 | "第 88 行存在 SQL 拼接，建议使用参数化查询" |
| **性能问题** | 循环嵌套、内存泄漏、N+1 查询 | "循环内执行数据库查询，建议批量获取" |
| **最佳实践** | 设计模式、SOLID 原则、可维护性 | "建议将此逻辑抽取为独立服务，遵循单一职责" |

### 4.2 上下文获取策略

```
┌─────────────────────────────────────────────────────┐
│                   上下文层级                          │
├─────────────────────────────────────────────────────┤
│  Level 1: PR 元数据                                  │
│  - 标题、描述、标签                                   │
│  - Review 评论历史                                   │
├─────────────────────────────────────────────────────┤
│  Level 2: 代码变更                                   │
│  - Diff 内容                                        │
│  - 变更文件列表                                      │
├─────────────────────────────────────────────────────┤
│  Level 3: 完整文件                                   │
│  - 变更文件的完整内容                                 │
│  - 相关依赖文件                                      │
├─────────────────────────────────────────────────────┤
│  Level 4: 项目上下文                                 │
│  - README、项目结构                                  │
│  - 历史 PR 模式                                      │
└─────────────────────────────────────────────────────┘
```

### 4.3 误报与漏报控制

- **置信度评分**: 每个问题标注置信度（高/中/低）
- **分级展示**: 高置信度问题优先展示
- **用户反馈**: 支持标记"误报"，用于后续优化
- **上下文增强**: 获取更多上下文减少误判

---

## 五、系统架构

```
┌──────────────────────────────────────────────────────────┐
│                      用户界面层                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │  Web 前端   │  │  CLI 工具   │  │  GitHub App │       │
│  │  (React)    │  │  (Python)   │  │  (Webhook)  │       │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘       │
└─────────┼────────────────┼────────────────┼───────────────┘
          │                │                │
          ▼                ▼                ▼
┌──────────────────────────────────────────────────────────┐
│                      API 网关层                            │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              FastAPI Application                     │ │
│  │  - 认证中间件                                        │ │
│  │  - 请求限流                                          │ │
│  │  - 错误处理                                          │ │
│  └─────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌──────────────────────────────────────────────────────────┐
│                      服务层                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ GitHub 服务 │  │  AI 分析    │  │  报告服务   │       │
│  │ - PR 获取   │  │  服务       │  │  - 生成     │       │
│  │ - Diff 解析 │  │  - 模型调用 │  │  - 存储     │       │
│  │ - 文件获取  │  │  - 结果解析 │  │  - 查询     │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
└──────────────────────────────────────────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌──────────────────────────────────────────────────────────┐
│                      数据层                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │   SQLite    │  │  MiMo API   │  │ GitHub API  │       │
│  │  (本地DB)   │  │  (AI 模型)  │  │ (代码获取)  │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
└──────────────────────────────────────────────────────────┘
```

---

## 六、开发计划

### 阶段一：基础架构
- [x] 需求分析与文档编写
- [ ] 项目初始化（目录结构、依赖管理）
- [ ] 后端框架搭建（FastAPI）
- [ ] 数据库设计与初始化
- [ ] 前端项目初始化（React + TailwindCSS）

### 阶段二：核心功能
- [ ] GitHub API 集成（获取 PR 数据）
- [ ] AI 分析服务实现（MiMo 模型调用）
- [ ] PR 分析 API 开发
- [ ] 分析结果展示页面

### 阶段三：功能完善
- [ ] 用户认证系统
- [ ] 历史记录管理
- [ ] 报告详情页面优化
- [ ] 错误处理与边界情况

### 阶段四：优化与测试
- [ ] 性能优化
- [ ] UI/UX 优化
- [ ] 测试与调试
- [ ] 文档编写

---

## 七、已确认事项

| 事项 | 确认结果 |
|------|----------|
| 交互形式 | Web 应用 + CLI（先做 Web） |
| 后端语言 | Python (FastAPI) |
| 前端框架 | React + TailwindCSS |
| AI 模型 | MiMo（小米推理模型） |
| GitHub 认证 | Personal Access Token |
| 数据存储 | SQLite |
| MVP 范围 | 核心优先（PR 分析 + 报告展示） |
| 开发环境 | 本地开发 |
| MiMo API | 用户有官方 API 文档 |

## 八、MiMo API 配置

| 配置项 | 值 |
|--------|-----|
| Base URL | `https://token-plan-cn.xiaomimimo.com/v1` |
| 模型名称 | `mimo-v2.5-pro` |
| API 格式 | OpenAI 兼容格式 |
| 认证方式 | Bearer Token |

> 注：API Key 已获取，存储在环境变量中，不写入代码。

---

## 八、设计思路说明

### 8.1 模型选择
选择 MiMo 模型的原因：
- 国产模型，中文理解能力强
- 推理能力突出，适合代码分析场景
- 可能有更好的性价比

### 8.2 上下文获取方式
采用分层获取策略：
- 先获取 PR 元数据和 diff（快速概览）
- 按需获取完整文件（深度分析）
- 未来可扩展项目级上下文

### 8.3 未来扩展方向
- 支持更多代码托管平台（GitLab、Bitbucket）
- 多模型支持（Claude、GPT-4、本地模型）
- CI/CD 集成（自动触发分析）
- 团队协作与知识沉淀

---

*文档版本: v1.0*
*创建日期: 2025-05-29*
